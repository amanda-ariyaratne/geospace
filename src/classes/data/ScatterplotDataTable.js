import { isJsonArray } from "./JsonHelper";

export default class ScatterplotDataTable {
  constructor() {
    this.dataset = [];
    this.keys = [];
  }

  // setDataset(jsonData){
  //     jsonData.forEach(element => {

  //     });
  // }
}
